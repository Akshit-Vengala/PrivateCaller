<?xml version="1.0" encoding="utf-8"?>
<!--
    Floating incoming-call banner drawn over the foreground app via a
    TYPE_APPLICATION_OVERLAY window (full edition only). Fully custom, so it
    renders identically with icons in portrait AND landscape. Tapping the body
    opens the call screen; the round buttons answer / decline.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <LinearLayout
        android:id="@+id/overlay_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:clickable="true"
        android:focusable="true"
        android:background="@drawable/bg_overlay_banner"
        android:elevation="8dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="12dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="12dp"
            android:orientation="vertical">
            <TextView
                android:id="@+id/overlay_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />
            <TextView
                android:id="@+id/overlay_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#B3FFFFFF"
                android:textSize="13sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/overlay_decline"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginEnd="12dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/bg_overlay_decline"
            android:contentDescription="@string/notif_decline">
            <ImageView
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_notif_call_end"
                android:tint="#FFFFFF"
                android:contentDescription="@string/notif_decline" />
        </FrameLayout>

        <FrameLayout
            android:id="@+id/overlay_answer"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:clickable="true"
            android:focusable="true"
            android:background="@drawable/bg_overlay_answer"
            android:contentDescription="@string/notif_answer">
            <ImageView
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_call_answer"
                android:tint="#FFFFFF"
                android:contentDescription="@string/notif_answer" />
        </FrameLayout>
    </LinearLayout>
</FrameLayout>
